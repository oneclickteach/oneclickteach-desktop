export enum DarkMode {
  DARK = 'dark',
  LIGHT = 'light',
  SYSTEM = 'system',
}

export enum DirectionMode {
  LTR = 'ltr',
  RTL = 'rtl',
}

export enum ServerProvider {
  VAGRANT = 'vagrant',
  MIZBANCLOUD = 'mizbancloud',
}
